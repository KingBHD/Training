#include<stdio.h>

int sum(int a, int b) {
    return (a + b);
}
int sub(int a, int b) {
    return (a - b);
}
int multi(int a, int b) {
    return (a * b);
}

void myFunction() {
    printf("This is a function in header file!");
}